#!/bin/bash
python3 ./start_server.py # Wrapper script for executing ‘~/ad_server/start_server.py’ or just ./start_server.py when using render, as the root is already defaulted or explicitly set to ad_server
